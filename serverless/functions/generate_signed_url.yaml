handler: export/main.lambda_handler
role: arn:aws:iam::***REMOVED***:role/ok-origo-dataplatform-data-export-dev
events:
  - http:
      path: /
      method: post
      cors: true
      documentation: ${file(serverless/documentation/generate_signed_url.yaml)}
      authorizer: ${file(serverless/kc-authorizer.yaml)}