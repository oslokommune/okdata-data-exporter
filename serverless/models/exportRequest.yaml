name: exportRequest
description: A request containing bucket + key
contentType: application/json
schema:
  required:
    - bucket
    - key
  properties:
    bucket:
      type: string
    key:
      type: string