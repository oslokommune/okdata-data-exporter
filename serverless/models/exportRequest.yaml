name: exportRequest
description: A request containing bucket + key
contentType: application/json
schema:
  type: object
  required:
    - bucket
    - key
  properties:
    bucket:
      type: string
    key:
      type: string