name: exportResponse
description: A response containing a signed url
contentType: application/json
schema:
  type: array
  items:
    type: object
    required:
      - key
      - url
    properties:
      key:
        type: string
      url:
        type: string